// goal is to press button n then it will return the output of cmd
// so need to do 'node <file in backend>' with a press of a button somehow
import { useCallback, useState, useEffect } from "react";

export default function printOutput() {
    /*TODO*/
    const sendRequest = "";
    return (
        <div>
            <button onClick={sendRequest} >click me!</button>
        </div>
    );
}